export interface IMovie {
    title: string,
    releaseYear?: number,
    genre?: string,
    director?: string,
    description?: string,
    watched?: boolean,
    rating?: string,
    review?: string
}
